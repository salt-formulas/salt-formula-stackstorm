applications:
- stackstorm
classes:
- service.stackstorm.support
parameters:
  stackstorm:
    server:
      enabled: true
      bind:
        address: 0.0.0.0
      message_queue: 
        host: localhost
        port: 5672
        user: guest
        password: guest
        virtual_host: /
